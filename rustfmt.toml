unstable_features = true

max_width = 120
style_edition = "2024"
edition = "2021"

# do not collapse small struct definitions into a single line
struct_variant_width = 0

imports_granularity = "module"
reorder_imports = true
group_imports = "StdExternalCrate"
